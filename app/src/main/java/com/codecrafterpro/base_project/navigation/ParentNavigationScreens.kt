package com.codecrafterpro.base_project.navigation

import com.codecrafterpro.base_project.R

enum class ParentNavigationScreens(
    val route: String,
) {
    MAIN(
        route = "route_main"
    ),
    AUTH (
        route = "route_auth"
    )
}